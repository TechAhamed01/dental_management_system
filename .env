# PostgreSQL Database Configuration
POSTGRES_USER=dental
POSTGRES_PASSWORD=asdf@123
POSTGRES_DB=dental

# Redis Configuration
REDIS_PASSWORD=dummy-password

# Serverpod Execution Mode (e.g., development, docker)
# For the Dockerized team environment, we use 'docker' to ensure internal networking works.
SERVERPOD_MODE=docker
